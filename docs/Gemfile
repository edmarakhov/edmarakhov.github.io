source "https://rubygems.org"

# Use the standard jekyll-github-metadata but exclude it from local builds if needed
gem "jekyll", "3.10.0"
gem "github-pages", group: :jekyll_plugins
gem "wdm", ">= 0.1.0" if Gem.win_platform?

# Add this to fix the Ruby 3.4 specific error
gem "fiddle"
gem "webrick"